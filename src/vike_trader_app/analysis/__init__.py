"""analysis — optimizer + the anti-overfitting suite (the differentiator). Phase 3.

Modules (created during implementation, test-first):
    optimizer.py   grid / random / Bayesian parameter search
    validation.py  walk-forward, purged/combinatorial CV (via skfolio)
    overfit.py     deflated Sharpe, PBO, Monte Carlo, plain-language "overfit risk" verdict
    metrics.py     return, win-rate, max drawdown, Sharpe, profit factor, ...
"""
