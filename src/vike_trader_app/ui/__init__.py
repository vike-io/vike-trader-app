"""ui — PySide6 desktop app (MT4-like). Imports core; core never imports ui.

Modules (created during implementation):
    app.py     main window / application shell
    chart.py   pyqtgraph candlestick chart + bar-by-bar replay
    panels/    stats, params, data-load dialog, report
"""
